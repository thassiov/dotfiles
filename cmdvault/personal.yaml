name: Personal Tools
description: Personal workflow scripts and utilities

commands:
  # Quick system commands (portable, works in containers too)
  - name: top
    command: top
    args: ["-b", "-n", "1"]
    description: Show running processes (single snapshot)
