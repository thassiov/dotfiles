name: Grid Remote Access
description: SSH and remote command execution on grid.local hosts

commands:
  - name: grid ssh
    command: grid
    args: ["ssh"]
    description: SSH to a host by name (prompts for host)

  - name: grid exec
    command: grid
    args: ["exec"]
    description: Run command on all PVE hosts (prompts for command)
