
" Use F2 to create a new tab (Ctrl+t)
map <F2> :tabnew<CR>

" Use F3 to go to the previous tab (Ctrl+PgUp)
map <F3> :tabprevious<CR>

" Use F4 to go to the next tab (Ctrl+PgDw)
map <F4> :tabnext<CR>

" Remove navbar"
set gui=nonavigation

" Remove tabs"
set gui=notabs

" Set default colorscheme
colorscheme indigo-fx3

" leader key
map , <Leader>

nmap <Leader>gt :set gui=tabs<CR>

nmap <Leader>nn :set gui=none<CR>
